- **fix(sse):** an Anthropic OAuth `403 "Request not allowed"` no longer bans the Claude connection — it is a per-request refusal on an otherwise healthy token (200s before and after on the same connection), so it is now classified as the new non-terminal `request_rejected` error type instead of `forbidden`; previously a single such response flipped the only Claude connection to `banned` and every later request was short-circuited with "All 1 connection(s) banned by upstream" until an operator reconnected
